{
  "name": "@ideia/token-optimization",
  "private": true,
  "description": "Token Optimization Analytics — breakdown por fonte, ROI por token, deteccao de desperdicio, compressao inteligente e dashboard de eficiencia",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*",
    "@types/node": "*"
  }
}
