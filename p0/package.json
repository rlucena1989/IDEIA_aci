{
  "name": "@ideia/p0",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  }, "scripts": {
  "test": "node test/semantic/run-catalog.ts && node --test test/**/*.test.ts",
  "test:catalog": "node test/semantic/run-catalog.ts",
  "generate-validators": "node scripts/generate-contract-validators.mjs",
  "check-drift": "node scripts/generate-contract-validators.mjs --check"
 },
  "description": "P0 vertical slice - isolated skeleton for IDEIA_aci",
  "dependencies": {
    "ajv": "8.20.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
