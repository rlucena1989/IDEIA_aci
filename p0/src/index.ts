/**
 * P0 vertical slice - isolated skeleton
 *
 * This is the minimal executable base for P0.
 * No dependencies, no imports from packages/.
 */

export const version = "0.0.1";

export function hello(): string {
  return "P0 skeleton initialized";
}
